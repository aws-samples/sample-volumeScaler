apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "volumescaler.fullname" . }}-test-connection"
  labels:
    {{- include "volumescaler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "volumescaler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
